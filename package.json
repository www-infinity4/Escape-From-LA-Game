{
  "name": "escape-from-la-game",
  "version": "1.0.0",
  "description": "Snake Plissken's Escape – browser game wrapped for Android via Capacitor",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && cp index.html game.js www/",
    "android:build": "npm run build && npx cap add android && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/core": "^8.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.2.0"
  }
}
